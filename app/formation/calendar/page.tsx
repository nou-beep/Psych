"use client";
// Formation Calendar — reuses the shared calendar surface so events
// from the same underlying engine show in both Formation and
// Therapist views without code duplication. The Formation sidebar
// links here so the URL stays inside /formation.
export { default } from "@/app/calendar/page";
