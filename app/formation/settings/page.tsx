"use client";
// Formation Settings — Formation Portal entry point for the settings
// surface. Renders the internship-specific settings page (evaluator
// + institutional defaults) since those are the Formation-relevant
// settings; general appearance / portal switching lives at /settings.
export { default } from "@/app/settings/internship/page";
