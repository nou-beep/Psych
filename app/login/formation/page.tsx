import { LoginScreen } from "@/components/auth/LoginScreen";

export const metadata = { title: "Formation sign in — Eyla" };

export default function FormationLoginPage() {
  return <LoginScreen portal="formation" />;
}
